960픽셀 그리드는 웹 디자이너 사이에서 널리 사용되고 있다. 
그리드는 항목 간에 일관성 있는 비율과 영역 설정을 통해 전문적인 디자인을 가능하게 한다. 
지금까지 살펴본 내용을 다시 떠올려 보면 앞서 살펴본 세 개의 열을 포함하여 모두 그리드에 따라 구성했음을 알 수 있다.

그리드가 제약처럼 느껴질 수도 있지만 실제로 다음과 같은 역할을 한다.

# 다양한 디자인을 사용하는 여러 페이지 사이의 연속성을 만들 수 있다.
# 다양한 페이지에서 사용자가 정보의 위치를 예측하는 데 도움을 준다.
# 일관된 방식으로 사이트에 새로운 콘텐츠를 더 쉽게 추가할 수 있다.
# 사이트의 디자인 작업을 일관된 방식으로 협업할 수 있다.


가능한 레이아웃: 



960픽셀 폭, 12열 그리드

페이지의 너비는 960픽셀이고 12개의 동일한 크기의 열이 있는데 각 열의 너비는 60픽셀이다
각 열은 마진을 10픽셀로 설정했으므로 각 열 사이에는 20픽셀의 여백이 페이지의 왼쪽과 오른쪽 면에는 10픽셀의 여백이 생긴다.


940px
460px 460px
300px 300px 300px
220px 220px 220px 220px
140px 140px 140px 140px 140px 140px



300px 620px
220px 700px
460px 220px 220px
300px 300px 140px 140px
220px 220px 140px 140px 140px




CSS 프레임워크의 목적은 레이아웃 그리드, 폼 스타일, 페이지의 인쇄용 버전 등의 공통 작업을 더 쉽게 할 수 있는 코드를 제공하는 데 있다.
처음부터 CSS를 작성하지 않아도 프로젝트에 CSS 프레임워크 코드를 포함시킬 수 있다.

*장점
1. 동일한 작업에 대한 반복적인 코드 작성을 줄일 수 있다.
2. 다양한 브라우저 버전에서 이미 검증됐다(브라우저 버그를 피할 수 있다).

*단점
1. HTML 코드에서는 페이지의 표현을 제어해야 하므로 콘텐츠의 설명이 아니라 클래스 이름을 사용해야 한다.
2. 다양한 요구사항에 대한 충족을 위해 특정 웹 페이지에 필요한 것보다 더 많은 코드를 포함되어 있을 수 있다.
이렇나 경우르 일반적으로 '코드 블로트 code bloat(코드가 부풀려지는 현상)'라 한다.


그리드 기반 CSS 프레임워크는 다음과 같다.

960.gs
blueprintcss.org
lessframework.com
developer.yahoo.com/yui/grids/


960gs에서 grid_12는 12열 너비의 블록을 생성한다.
          grid_4는 4열 너비의 블록을 생성한다.

container_12는 전체 페이지에 대한 컨테이너처럼 동작하고 12열 그리드를 사용함을 나타낸다.


